# =============================================================================
# Databricks Genie Bot - Full Deployment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# The .env file is gitignored and will not be committed.
#
# Usage:
#   cp env.example .env
#   # Edit .env with your values
#   ./deploy-full.sh
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Azure Tenant Information
# -----------------------------------------------------------------------------
# Your Azure AD Tenant ID
# Find at: Azure Portal > Azure Active Directory > Overview > Tenant ID
TENANT_ID=your-tenant-id

# Azure region for all resources
LOCATION=eastus

# -----------------------------------------------------------------------------
# REQUIRED: Databricks Configuration
# -----------------------------------------------------------------------------
# Databricks workspace URL (e.g., https://adb-123456789.12.azuredatabricks.net)
DATABRICKS_HOST=https://your-workspace.azuredatabricks.net

# Genie Space ID - found in Databricks > Genie > Your Space > Settings
GENIE_SPACE_ID=your-genie-space-id

# -----------------------------------------------------------------------------
# OPTIONAL: Databricks OAuth App (for OBO token exchange)
# -----------------------------------------------------------------------------
# Create at: Databricks > Settings > Developer > OAuth applications
# Leave empty if not using OBO flow
DATABRICKS_CLIENT_ID=
DATABRICKS_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# OPTIONAL: Resource Naming (auto-generated if empty)
# -----------------------------------------------------------------------------
# If you want specific names, fill these in. Otherwise, the script will
# generate unique names based on a random suffix.
#
# Example: If BOT_NAME=genie-bot, the script will create:
#   - App Registration: genie-bot
#   - Bot Service: genie-bot
#   - Resource Group: genie-bot-rg
#   - Key Vault: genie-bot-kv
#   - App Service: genie-bot-app

BOT_NAME=
RESOURCE_GROUP=
KEY_VAULT_NAME=
APP_SERVICE_PLAN=
APP_NAME=

# -----------------------------------------------------------------------------
# AUTO-GENERATED: Do NOT fill these in manually
# -----------------------------------------------------------------------------
# These values are generated by the deploy-full.sh script and saved to
# .env.generated for reference. Do not set them here.
#
# MICROSOFT_APP_ID=       (created by script)
# MICROSOFT_APP_PASSWORD= (created by script)
# KEY_VAULT_URL=          (derived from KEY_VAULT_NAME)
# BOT_PUBLIC_URL=         (derived from APP_NAME)
